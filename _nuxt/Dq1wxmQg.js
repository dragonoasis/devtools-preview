const e="HTML to JSX Converter — Paste Markup, Get React",t="Convert HTML into valid JSX: class becomes className, inline styles become objects, void elements self-close, and comments are converted.",a="HTML",n="Paste HTML markup",o="Wrap in a component",s="Component name",i="Could not convert this HTML.",r="Parsing and conversion run entirely in your browser; the markup is never uploaded.",l={heading:"Why HTML Is Not Valid JSX",content:"JSX looks like HTML but is compiled to JavaScript, so several things differ. Attribute names that collide with JavaScript reserved words are renamed, most visibly class to className and for to htmlFor. Multi-word attributes become camelCase, while data- and aria- attributes keep their hyphens. Inline style must be an object rather than a string. Void elements such as img and br must be explicitly self-closed. Comments become JavaScript expressions, literal braces must be escaped, and sibling roots need a fragment wrapper. This converter applies all of those rules with its own parser, so the result is deterministic rather than whatever the browser DOM decided to repair."},c={heading:"How to Convert Markup",steps:["Paste an HTML snippet: a template fragment, an email layout, or markup copied from a design handoff.","Read the converted JSX, which updates as you type.","Turn on component wrapping to get a complete exported function component instead of a bare fragment.","Give the component a name; an unusable name falls back to a valid identifier rather than emitting broken JavaScript.","Copy the result into your project and replace inline event handler strings with real functions."]},h={heading:"Where This Comes Up",items:[{title:"Port a static template",description:"Move an existing HTML page or partial into a React codebase without hand-editing every attribute."},{title:"Use a snippet from documentation",description:"Component libraries and CSS frameworks publish plain HTML examples; convert them before pasting into a component."},{title:"Migrate an email or landing page",description:"Convert table-heavy markup with inline styles, which is the most tedious kind to translate by hand."},{title:"Fix a build error fast",description:"When a paste fails to compile, run it through here to see exactly which attributes and self-closing rules were wrong."}]},d=[{question:"Why are data- and aria- attributes left alone?",answer:"React passes those through to the DOM verbatim, so they keep their hyphens. Only attributes React defines a camelCase property for are renamed, which is why data-test-id stays as it is while tabindex becomes tabIndex."},{question:"What happens to inline event handlers?",answer:"An attribute such as onclick is renamed to onClick, but its value stays a string. That is intentional: a string is not a valid handler in React, so the output makes the remaining work obvious rather than inventing a function that might not match your code."},{question:"Why did my markup get wrapped in <> and </>?",answer:"JSX expressions must have a single root. When the input has several top-level elements, the converter adds a fragment so the result compiles. If you only need one root, convert that element on its own."},{question:"Does the converter handle malformed HTML?",answer:"It ignores stray closing tags the way a browser does, but reports unterminated tags and comments rather than guessing. It does not run a full HTML5 tree-construction algorithm, so deliberately broken markup may need cleaning first."}],m=["html-to-text-markdown-converter","html-entity-encoder-decoder","code-formatter","html-wysiwyg-editor"],p={title:e,description:t,inputLabel:a,inputPlaceholder:n,asComponent:o,componentName:s,failed:i,privacy:r,whatIs:l,howToUse:c,useCases:h,faq:d,relatedTools:m};export{o as asComponent,s as componentName,p as default,t as description,i as failed,d as faq,c as howToUse,a as inputLabel,n as inputPlaceholder,r as privacy,m as relatedTools,e as title,h as useCases,l as whatIs};
